---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-16.04

suites:
  - name: default
    run_list:
      - recipe[workflowstats::install_dependencies]
      - recipe[workflowstats::setup_code]
    attributes:
